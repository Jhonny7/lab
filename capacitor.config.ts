import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mx.com.olam.systems.lab',
  appName: 'Lab',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
